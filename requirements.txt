streamlit>=1.30
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
requests>=2.28

# Opcionais — cada módulo cai para um fallback próprio caso não estejam instalados
hmmlearn>=0.3      # módulo 2 (regimes via HMM) — sem isso, usa GaussianMixture
ruptures>=1.1      # módulo 3 (changepoint) — sem isso, usa segmentação binária própria
